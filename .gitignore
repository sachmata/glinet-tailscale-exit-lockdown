# Runtime/state artifacts (never part of the repo)
*.state
*.lock

# Local captures, scratch, and any pulled live router config
live-state/
*.local
*.bak

# Local resolver override (lives on the router; keep private IPs out of git)
ts-lockdown.conf

# Editor/OS noise
.DS_Store
*~
.idea/
.vscode/
